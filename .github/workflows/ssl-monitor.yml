name: SSL Certificate Monitor

on:
  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  check-ssl:
    runs-on: ubuntu-latest

    steps:

      # ============================================================
      # 1. CHECK SSL CERTIFICATE
      # ============================================================
      - name: Check SSL certificate
        shell: bash
        run: |
          DOMAIN="app.cmercury.com"
          EXPECTED_DOMAIN="app.cmercury.com"
          SERVER_IP="208.100.49.130"

          CERT_FILE=$(mktemp)

          echo "=========================================="
          echo "SSL CERTIFICATE CHECK"
          echo "=========================================="
          echo "Domain: $DOMAIN"
          echo "Server IP: $SERVER_IP"
          echo "Expected hostname: $EXPECTED_DOMAIN"
          echo ""

          # --------------------------------------------------------
          # Retrieve the public certificate using SNI
          # --------------------------------------------------------

          echo | openssl s_client \
            -connect "${DOMAIN}:443" \
            -servername "${DOMAIN}" \
            -showcerts 2>/dev/null \
            | openssl x509 -out "$CERT_FILE" 2>/dev/null

          # --------------------------------------------------------
          # Certificate could not be retrieved
          # --------------------------------------------------------

          if [ ! -s "$CERT_FILE" ]; then

            echo "SSL_CHECK_FAILED=true" >> "$GITHUB_ENV"
            echo "SSL_ALERT_TYPE=SSL Connection Failed" >> "$GITHUB_ENV"
            echo "SSL_MESSAGE=Unable to retrieve SSL certificate from ${DOMAIN}:443." >> "$GITHUB_ENV"

            echo "SSL_SUBJECT=Certificate unavailable" >> "$GITHUB_ENV"
            echo "SSL_ISSUER=Unknown" >> "$GITHUB_ENV"
            echo "SSL_EXPIRY=Unknown" >> "$GITHUB_ENV"
            echo "SSL_NOT_BEFORE=Unknown" >> "$GITHUB_ENV"
            echo "SSL_SERIAL=Unknown" >> "$GITHUB_ENV"

            echo "=========================================="
            echo "SSL CONNECTION FAILED"
            echo "=========================================="

            exit 0
          fi

          # --------------------------------------------------------
          # Read certificate information
          # --------------------------------------------------------

          SUBJECT=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -subject | sed 's/^subject=//')

          ISSUER=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -issuer | sed 's/^issuer=//')

          EXPIRY=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -enddate | cut -d= -f2)

          NOT_BEFORE=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -startdate | cut -d= -f2)

          SERIAL=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -serial | cut -d= -f2)

          SAN=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -ext subjectAltName 2>/dev/null || true)

          echo "SSL_SUBJECT=$SUBJECT" >> "$GITHUB_ENV"
          echo "SSL_ISSUER=$ISSUER" >> "$GITHUB_ENV"
          echo "SSL_EXPIRY=$EXPIRY" >> "$GITHUB_ENV"
          echo "SSL_NOT_BEFORE=$NOT_BEFORE" >> "$GITHUB_ENV"
          echo "SSL_SERIAL=$SERIAL" >> "$GITHUB_ENV"

          echo "Certificate Subject:"
          echo "$SUBJECT"
          echo ""

          echo "Certificate Issuer:"
          echo "$ISSUER"
          echo ""

          echo "Valid From:"
          echo "$NOT_BEFORE"
          echo ""

          echo "Valid Until:"
          echo "$EXPIRY"
          echo ""

          echo "Serial:"
          echo "$SERIAL"
          echo ""

          echo "Subject Alternative Names:"
          echo "$SAN"
          echo ""

          # --------------------------------------------------------
          # Check certificate expiry
          # --------------------------------------------------------

          if ! openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -checkend 0 >/dev/null 2>&1; then

            echo "SSL_CHECK_FAILED=true" >> "$GITHUB_ENV"
            echo "SSL_ALERT_TYPE=SSL Certificate Expired" >> "$GITHUB_ENV"
            echo "SSL_MESSAGE=The SSL certificate for ${DOMAIN} has expired." >> "$GITHUB_ENV"

            echo "=========================================="
            echo "SSL CERTIFICATE EXPIRED"
            echo "=========================================="

            rm -f "$CERT_FILE"
            exit 0
          fi

          # --------------------------------------------------------
          # Check hostname
          #
          # This checks whether the certificate is actually valid
          # for app.cmercury.com.
          #
          # We do NOT require the CN to be *.cmercury.com.
          # The certificate can have another CN if its SAN covers
          # app.cmercury.com.
          # --------------------------------------------------------

          HOST_CHECK=$(openssl x509 \
            -in "$CERT_FILE" \
            -noout \
            -checkhost "$EXPECTED_DOMAIN" 2>&1)

          HOST_STATUS=$?

          echo "Hostname validation result:"
          echo "$HOST_CHECK"
          echo ""

          if [ "$HOST_STATUS" -ne 0 ]; then

            echo "SSL_CHECK_FAILED=true" >> "$GITHUB_ENV"
            echo "SSL_ALERT_TYPE=Wrong SSL Certificate" >> "$GITHUB_ENV"

            echo "SSL_MESSAGE=The certificate presented by ${DOMAIN} does not cover ${EXPECTED_DOMAIN}. Presented certificate: ${SUBJECT}" >> "$GITHUB_ENV"

            echo "=========================================="
            echo "WRONG SSL CERTIFICATE"
            echo "=========================================="
            echo "Expected hostname: $EXPECTED_DOMAIN"
            echo "Presented certificate: $SUBJECT"
            echo "=========================================="

            rm -f "$CERT_FILE"
            exit 0
          fi

          # --------------------------------------------------------
          # SSL is valid
          # --------------------------------------------------------

          echo "SSL_CHECK_FAILED=false" >> "$GITHUB_ENV"
          echo "SSL_ALERT_TYPE=SSL Certificate OK" >> "$GITHUB_ENV"
          echo "SSL_MESSAGE=SSL certificate is valid and covers ${EXPECTED_DOMAIN}." >> "$GITHUB_ENV"

          echo "=========================================="
          echo "SSL CERTIFICATE OK"
          echo "=========================================="
          echo "Certificate: $SUBJECT"
          echo "Hostname: $EXPECTED_DOMAIN"
          echo "Status: VALID"
          echo "=========================================="

          rm -f "$CERT_FILE"


      # ============================================================
      # 2. SEND EMAIL THROUGH EMAILJS
      # ============================================================

      - name: Send SSL Alert through EmailJS
        if: env.SSL_CHECK_FAILED == 'true'
        shell: bash
        run: |

          echo "=========================================="
          echo "SENDING SSL ALERT THROUGH EMAILJS"
          echo "=========================================="

          RESPONSE=$(curl --silent --show-error \
            --write-out "\nHTTP_STATUS:%{http_code}" \
            --request POST \
            "https://api.emailjs.com/api/v1.0/email/send" \
            --header "Content-Type: application/json" \
            --data "$(jq -n \
              --arg service_id "${{ secrets.EMAILJS_SERVICE_ID }}" \
              --arg template_id "${{ secrets.EMAILJS_TEMPLATE_ID }}" \
              --arg user_id "${{ secrets.EMAILJS_PUBLIC_KEY }}" \
              --arg accessToken "${{ secrets.EMAILJS_PRIVATE_KEY }}" \
              --arg domain "app.cmercury.com" \
              --arg server_ip "208.100.49.130" \
              --arg alert_type "${{ env.SSL_ALERT_TYPE }}" \
              --arg message "${{ env.SSL_MESSAGE }}" \
              --arg certificate_subject "${{ env.SSL_SUBJECT }}" \
              --arg certificate_issuer "${{ env.SSL_ISSUER }}" \
              --arg valid_from "${{ env.SSL_NOT_BEFORE }}" \
              --arg valid_until "${{ env.SSL_EXPIRY }}" \
              --arg expected_hostname "app.cmercury.com" \
              --arg expected_certificate "cmercury.com" \
              '{
                service_id: $service_id,
                template_id: $template_id,
                user_id: $user_id,
                accessToken: $accessToken,
                template_params: {
                  domain: $domain,
                  server_ip: $server_ip,
                  alert_type: $alert_type,
                  message: $message,
                  certificate_subject: $certificate_subject,
                  certificate_issuer: $certificate_issuer,
                  valid_from: $valid_from,
                  valid_until: $valid_until,
                  expected_hostname: $expected_hostname,
                  expected_certificate: $expected_certificate
                }
              }')")

          echo "$RESPONSE"

          HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)

          echo ""
          echo "HTTP Status: $HTTP_STATUS"

          if [ "$HTTP_STATUS" != "200" ]; then
            echo "=========================================="
            echo "EMAILJS EMAIL FAILED"
            echo "=========================================="
            exit 1
          fi

          echo "=========================================="
          echo "SSL ALERT EMAIL SENT SUCCESSFULLY"
          echo "=========================================="
